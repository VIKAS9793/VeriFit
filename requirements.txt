# VeriFit - Python Dependencies
# Generated: 2025-12-28
# Python Version Required: 3.11+
# Compliance: SYSTEM.md Section 1.1 (Technology Research) & Section 9 (Engineering Standards)

# ========================================
# Core Orchestration (Section 2)
# ========================================
# LangGraph v1.x - HITL-native orchestration
langgraph>=1.0.0,<2.0.0

# LangChain v1.x - Production-ready LLM framework
langchain>=1.0.0,<2.0.0
langchain-core>=1.0.0,<2.0.0
langchain-community>=0.3.0,<0.5.0  # Still in v0.x series

# LangChain integrations (to be specified based on LLM provider)
# langchain-openai>=1.0.0,<2.0.0  # Uncomment if using OpenAI
# langchain-google-genai>=1.0.0,<2.0.0  # Uncomment if using Google AI

# ========================================
# Resume Parsing & NLP (Section 3, Section 5)
# ========================================
# PDF parsing
pypdf>=4.0.0,<5.0.0
pdfplumber>=0.11.0,<1.0.0

# Document parsing (DOCX, TXT)
python-docx>=1.0.0,<2.0.0

# NLP for deterministic parsing
spacy>=3.7.0,<4.0.0
# Run: python -m spacy download en_core_web_sm

# Named Entity Recognition
spacy-transformers>=1.3.0,<2.0.0  # Optional: for advanced NER

# ========================================
# Semantic Similarity (Section 3)
# ========================================
# Sentence-BERT for embeddings
sentence-transformers>=2.3.0,<3.0.0

# ========================================
# Data Validation & Schemas
# ========================================
pydantic>=2.5.0,<3.0.0  # Type-safe data models
pydantic-settings>=2.1.0,<3.0.0  # Environment variable management
email-validator>=2.3.0,<3.0.0  # Email validation for Pydantic EmailStr

# ========================================
# Web Framework (API Layer)
# ========================================
flask>=3.0.0
flask-cors>=4.0.0
bleach>=6.0.0
python-magic-bin>=0.4.14
google-genai>=1.0.0
tenacity>=8.2.0

# fastapi>=0.109.0,<1.0.0  # Modern async API framework (Future)
# uvicorn[standard]>=0.27.0,<1.0.0  # ASGI server

# ========================================
# Database & Storage
# ========================================
sqlalchemy>=2.0.0,<3.0.0  # ORM for database
alembic>=1.13.0,<2.0.0  # Database migrations

# SQLite for development (built-in with Python)
# PostgreSQL driver for production
psycopg2-binary>=2.9.9,<3.0.0

# ========================================
# Privacy & Encryption (Section 7)
# ========================================
cryptography>=42.0.0,<43.0.0  # Encryption at rest
python-dotenv>=1.0.0,<2.0.0  # Environment variable loading

# ========================================
# HTTP & API Clients
# ========================================
httpx>=0.26.0,<1.0.0  # Async HTTP client for job scraping
requests>=2.31.0,<3.0.0  # Sync HTTP client (fallback)

# ========================================
# Utilities
# ========================================
python-dateutil>=2.8.2,<3.0.0  # Date parsing (for job postings)
pyyaml>=6.0.1,<7.0.0  # YAML config files
loguru>=0.7.2,<1.0.0  # Enhanced logging

# ========================================
# Testing & Quality (Section 8, Section 9)
# ========================================
pytest>=7.4.0,<8.0.0  # Testing framework
pytest-asyncio>=0.23.0,<1.0.0  # Async test support
pytest-cov>=4.1.0,<5.0.0  # Coverage reporting
pytest-mock>=3.12.0,<4.0.0  # Mocking utilities

# Code quality
black>=24.1.0,<25.0.0  # Code formatter
ruff>=0.1.0,<1.0.0  # Fast linter (replaces flake8, isort)
mypy>=1.8.0,<2.0.0  # Type checking

# ========================================
# Development Tools
# ========================================
ipython>=8.20.0,<9.0.0  # Enhanced REPL
ipdb>=0.13.13,<1.0.0  # Debugger

# ========================================
# Governance & Auditing (Section 5)
# ========================================
# (To be added: bias detection libraries)

# ========================================
# Notes
# ========================================
# 1. All versions use "compatible release" (~=) or range (<) for patch/minor updates
# 2. Major versions locked to prevent breaking changes
# 3. See docs/TECHNOLOGY_RESEARCH.md for version selection rationale
# 4. Run: pip install -r requirements.txt
# 5. For production, consider using pip-compile (pip-tools) for exact pins
